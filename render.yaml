services:
  - type: web
    name: fredora-multiconcept
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile=false && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        value: postgresql://postgres:zicmercy94260@db.mzhfenzuxrnwocfpolgq.supabase.co:5432/postgres
